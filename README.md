# chatgpt-thread-archiver
Browser userscript that archives the open ChatGPT thread as one self-contained offline HTML file. Uses your signed-in session instead of scraping the page, so nothing is lost to lazy loading. Sends a bearer token to ChatGPT's internal endpoint.
